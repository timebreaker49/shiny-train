version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
  web:
    build: ./quotesfrontend
    ports:
      - "3000:3000"
    volumes:
      - ./quotesfrontend:/quotesfrontend